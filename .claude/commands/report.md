Generate the interactive detection report in background.

Run this command in background:
```
venv/bin/python -m frame_prep.cli report $ARGUMENTS
```

When complete, tell the user the report is ready and output the path as a clickable file:// URL: `file:///home/antoine/stuff/picture-frame-preprocessor/reports/interactive_detection_report.html`
